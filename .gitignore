# OS
.DS_Store

# Dependencies
node_modules/

# Next.js
.next/
out/

# Production build
build/

# Environment
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Prototype-publishing pipeline (migrated from prototypes repo)
pipeline/.env
pipeline/.tmp/
pipeline/node_modules/

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Pinterest board images (98 pins, ~50MB)
# Downloaded for visual analysis — reference only, not source code
research/pinterest-board/

# ASMV-3 source research — consolidated into 03-graphics-pipeline-final.md
# Kept locally for reference, not tracked in repo
research/03-computational-graphics-pipeline.md
research/03-platform-research-notes.md
research/03-visual-analysis-notes.md
research/03-gemini-imagen-research.md

# Raw photo inbox (contents never committed, only .gitkeep tracked)
_raw/*
!_raw/.gitkeep
.claude/worktrees/

# Sibling worktrees created by scripts/new-lattice-worktree.sh
# (siblings live outside the repo so this is belt-and-braces)
worktree-*/

# Source portfolio PDF (33MB) — redundant with rendered book images; host separately
public/roberts-selected-works-2022-2024.pdf
