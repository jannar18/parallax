# ASMV-25: Review /update skill & artifact generation pipeline

## Scope
Review three artifacts and record findings as a Lattice comment:
1. `notes/update-skill-steps.md` — the manual process spec from the practice run
2. `~/.claude/commands/update.md` — the existing `/update` command (ASMV-22)
3. `src/lib/content.ts` + existing `src/content/now/` entries — the content schema and examples

## Acceptance Criteria
- Lattice comment documenting: what works, what's missing, what needs refinement
- Clear recommendations for ASMV-18 implementation
